org.gradle.jvmargs=-Xmx2G

# Platform. Every module is built against exactly these versions; nothing
# overrides them locally, so the two mods can never drift apart.
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.19.5
loom_version=1.8.12
fabric_version=0.116.17+1.21.1
java_version=21

# Shared by every module. A module sets only its own archive name.
mod_version=1.0.0
maven_group=com.josuebrenes
